# test/ — ai_workflow-Generated Test Drafts

These files were auto-generated by `ai_workflow.js` and are **not currently run by Jest**.

## Why they don't run yet

The active Jest config (`package.json`) matches `**/tests/**/*.test.js` (note: `tests/`, not `test/`).
These drafts use ESM `import` syntax and expect source files to behave as ES modules — but
`overlay.js` and `popup.js` are IIFE/browser-script style without exports.

## Status per file

| File | Blocker | Adapted version |
|------|---------|-----------------|
| `background/service-worker.test.js` | ESM import → converted to CJS | ✅ `tests/service-worker.test.js` |
| `content/overlay.test.js` | overlay.js is an IIFE, not a module | ⏳ Roadmap — requires module refactor |
| `popup/popup.js` | popup.js has no exports, DOM side-effects | ⏳ Roadmap — requires module refactor |

## To enable content/overlay and popup tests

Refactor `overlay.js` and `popup.js` to export their pure functions (separate module files),
so tests can import and exercise them without a browser DOM context.
See roadmap — *Milestone 3: Module refactor for full unit test coverage*.
